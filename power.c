include<stdio.h>
int main()
{
int pow,num,sum=1;
printf("\n enter the number");
scanf("%d",&num);
printf("\n enter power of a number");
scanf("%d",&pow);
while(i<=power)
{
sum=sum*num;
i++;
}
printf("\n %d to the power is %d:%1d,num,sum);
return 0;
}
