#include<stdio.h>
#include<conio.h>
int main()
{
char str[100]
int=0,word=0;
printf("enter the number of string");
scanf("%d",&n);
gets(string)
while(string[i]!='\0')
if(string[i]==' '\\ string[i]=='\n'\\ string[i]=='\t')
{
i++    
}
printf("enter the total number of string"\t);
return 0;
}
